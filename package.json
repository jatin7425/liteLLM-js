{
  "name": "litellm-proxy-vercel",
  "version": "1.0.0",
  "type": "module",
  "main": "api/index.js",
  "scripts": {
    "dev": "node --env-file=.env server.js",
    "start": "node --env-file=.env server.js",
    "vercel:dev": "vercel dev",
    "deploy": "vercel deploy --prod",
    "test": "node test-proxy.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.0",
    "js-yaml": "^4.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "vercel": "^latest"
  },
  "engines": {
    "node": "24.x"
  }
}
